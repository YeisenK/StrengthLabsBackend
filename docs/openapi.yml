# NOTA: Este archivo está OBSOLETO.
#
# La especificación OpenAPI se genera automáticamente por springdoc-openapi
# a partir de las anotaciones del código fuente.
#
# Con el backend corriendo en localhost:8000:
#
#   Swagger UI (interactivo):  http://localhost:8000/swagger-ui/index.html
#   JSON spec:                 http://localhost:8000/v3/api-docs
#   YAML spec:                 http://localhost:8000/v3/api-docs.yaml
#
# Para regenerar este archivo:
#   curl http://localhost:8000/v3/api-docs.yaml > docs/openapi.yml
#
# El archivo estático que había aquí describía el SessionController viejo
# (borrado en commit 566587f) y el schema plano de sesiones.
# El modelo actual es jerárquico: workout → workout_exercises → workout_sets.
